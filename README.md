# Vue Rabbit

基于 `Vue 3 + Vite + Pinia + Element Plus` 的电商前台单页应用，覆盖首页推荐、分类浏览、商品详情、SKU 选购、购物车、结算下单、支付回调和会员中心等完整业务链路，适合作为前端实习求职作品展示。

## 在线体验

- 线上地址：[vue-rabbit-five.vercel.app](https://vue-rabbit-five.vercel.app/)

## 项目定位

- 项目类型：B2C 电商前台 SPA
- 项目目标：还原真实电商核心链路，并沉淀可复用的状态管理、接口封装与交互组件能力
- 适用场景：前端实习简历项目、Vue 3 工程化练习、业务型作品集展示

## 核心亮点

1. 基于 Vue 3 组合式 API 搭建电商业务模块，完成从浏览商品到支付回调的完整前台链路。
2. 封装 `Axios` 请求实例与响应拦截器，统一处理鉴权失效、错误提示和登录回跳。
3. 使用 `Pinia + pinia-plugin-persistedstate` 管理用户态与购物车数据，支持游客态持久化与登录后购物车合并。
4. 实现 SKU 规格联动逻辑，通过幂集算法构建路径字典，支持无库存规格禁用。
5. 基于 `IntersectionObserver` 自定义图片懒加载指令，并为首页补充骨架屏、失败重试和空态反馈，提升首屏体验。
6. 使用路由懒加载、全局前置守卫和动态标题管理，提升页面性能与访问控制完整性。

## 功能概览

| 模块 | 功能说明 |
|------|----------|
| 首页 | 分类导航、Banner、热卖推荐、楼层商品展示 |
| 分类页 | 一级分类、二级分类、商品筛选、列表加载 |
| 商品详情 | 图片预览、规格选择、数量选择、加入购物车 |
| 购物车 | 单选/全选、数量修改、删除、价格汇总 |
| 结算页 | 收货地址、配送方式、支付方式、提交订单 |
| 支付页 | 订单倒计时、支付跳转、支付结果回调 |
| 会员中心 | 用户信息、订单列表、状态切换 |
| 登录页 | 表单校验、测试账号快捷填充、登录回跳 |

## 业务流程图

```mermaid
flowchart LR
    A["首页 / 分类浏览"] --> B["商品详情页"]
    B --> C["SKU 选择 + 加入购物车"]
    C --> D["购物车页"]
    D --> E{"是否已登录"}
    E -- "否" --> F["登录页"]
    F --> G["登录后合并本地购物车"]
    G --> H["结算页"]
    E -- "是" --> H["结算页"]
    H --> I["提交订单"]
    I --> J["支付页 / 支付回调"]
    J --> K["会员中心订单页"]
```

## 技术栈

- 框架：`Vue 3.5`、`Vite 8`
- 路由：`Vue Router 5`
- 状态管理：`Pinia 3`、`pinia-plugin-persistedstate`
- UI 组件：`Element Plus`
- 网络请求：`Axios`
- 工具库：`VueUse`、`Day.js`
- 样式方案：`SCSS`
- 代码质量：`ESLint`、`Oxlint`

## 我做了什么

- 按业务拆分接口层、状态层、页面层和组合式逻辑，提升代码可维护性。
- 抽离 `useAsyncData` 统一处理异步请求的 `loading / error / retry` 状态。
- 优化首页首屏体验，增加骨架屏、失败重试和更完整的购物车浮层空态。
- 处理登录鉴权流程，支持未登录访问受限页面时自动跳转登录并回跳原页面。
- 修复并完善工程配置，使 `npm run lint` 与 `npm run build` 可稳定通过。

## 简历描述参考

### 一句话版本

使用 Vue 3 + Vite 独立完成电商前台 SPA，涵盖商品浏览、SKU 选择、购物车、结算下单与支付回调，并通过 Pinia、Axios 拦截器、路由守卫和懒加载等方案提升状态管理、权限控制与页面体验。

### 两到三条亮点版本

- 基于 Vue 3 组合式 API 与 Pinia 搭建电商业务前台，完成首页、分类、商品详情、购物车、结算、支付和会员中心等核心页面开发。
- 封装 Axios 请求实例与全局拦截器，统一处理认证失效、错误提示和登录回跳逻辑，提升接口层可维护性。
- 实现 SKU 规格联动、游客购物车本地持久化与登录后购物车合并，并通过自定义图片懒加载、路由懒加载和骨架屏优化首屏体验。

## 本地运行

### 环境要求

- Node.js `^22.18.0` 或 `>=24.12.0`

### 安装依赖

```bash
npm install
```

### 环境变量

复制 `.env.example` 为 `.env`，按需配置：

| 变量名 | 说明 |
|--------|------|
| `VITE_API_BASE_URL` | 接口基础地址 |
| `VITE_PAY_BASE_URL` | 支付跳转基础地址 |

### 启动开发环境

```bash
npm run dev
```

### 构建生产包

```bash
npm run build
```

### 代码检查

```bash
npm run lint
```

### 运行测试

```bash
npm test
```

当前已包含基于 Node 原生测试运行器的基础用例，覆盖购物车结算前校验、下单 payload 组装、登录表单预处理和地址表单校验等关键逻辑。

## 测试账号

| 字段 | 值 |
|------|----|
| 账号 | `heima282` |
| 密码 | `hm#qd@23!` |

## 测试覆盖说明

- 购物车结算前校验：空购物车、未勾选商品、可正常结算三种状态
- 下单流程关键逻辑：订单提交 payload 只保留 `skuId` 与 `count`，并对买家留言做 trim
- 登录后购物车合并：本地购物车合并 payload 只保留 `skuId`、`selected` 和 `count`
- 表单校验：登录输入预处理、地址表单手机号与邮编校验

测试文件位于 `test/` 目录，可直接通过 `npm test` 执行。

## 项目结构

```text
src
├─ apis          # 按业务模块拆分接口
├─ components    # 全局组件
├─ composables   # 可复用组合式逻辑
├─ directives    # 自定义指令
├─ router        # 路由与鉴权
├─ stores        # Pinia 状态管理
├─ styles        # 全局样式变量
├─ utils         # 请求封装与工具函数
└─ views         # 页面模块
```

## 截图建议

如果你要把仓库公开给面试官，建议补充以下截图放在 `docs/screenshots/`：

1. 首页首屏：分类导航 + Banner + 卖点信息
2. 商品详情页：SKU 联动 + 放大镜预览
3. 购物车页：价格汇总 + 勾选逻辑
4. 结算页：地址/配送/支付方式
5. 会员中心：订单列表与状态切换

## 部署建议

当前项目已部署到 [Vercel](https://vercel.com/)，线上访问地址为：[vue-rabbit-five.vercel.app](https://vue-rabbit-five.vercel.app/)。

后续如果继续优化页面体验、补充截图或修复交互细节，可以直接复用当前部署链路持续更新演示版本。

## 性能优化记录

- 登录页移除了原先约 `1 MB` 的大背景图引用，改为渐变叠加较小背景资源，降低首次加载资源体积。
- 首页图片通过自定义 `v-img-lazy` 指令延迟加载，减少首屏不必要的图片请求。
- 首页异步模块增加骨架屏与失败重试，改善弱网环境下的感知性能。
- 页面采用路由懒加载，降低首次进入时的主包压力。

## License

MIT
